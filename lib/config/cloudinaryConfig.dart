class CloudinaryConfig {
  // Cloudinary credentials for unsigned uploads
  static const String cloudName = 'dd9sxpz8r';
  static const String uploadPreset = 'napimagebucket';

  // Environment-specific configurations
  static const bool useSignedUploads = false; // Using unsigned uploads for now
}